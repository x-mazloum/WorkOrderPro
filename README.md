# WorkOrderPro

WorkOrderPro uses a separated frontend/backend architecture to simulate a real production SaaS team workflow. The Next.js frontend is responsible for routing, UI, dashboard views, responsive technician workflows, and API consumption. The Express backend owns authentication, RBAC, business rules, work order lifecycle logic, file uploads, realtime events, background jobs, and MySQL persistence through Prisma.
